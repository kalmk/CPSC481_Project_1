## Requirements

- Python 3.9 or higher
- Windows
- pip (comes with Python)

## Quick Start

Open **Command Prompt** in the project folder and run:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python diagnostics_gui.py
